{
  "name": "rastogi-coders",
  "version": "1.0.0",
  "private": true,
  "description": "Rastogi Coders company website - React + Node.js",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "excel:pricing": "node scripts/generate-pricing-excel.mjs"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
